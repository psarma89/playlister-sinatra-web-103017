<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <form class="" action="/songs/<%=@song.slug%>" method="post">
      <input type="hidden" name="_method" value="patch">
      <p>Name: <input id = "Name" type="text" name="song_name" value="<%= @song.name %>"></p>
      <p>Artist Name: <input id= "Artist Name" type="text" name="artist_name" value="<%= @song.artist.name %>"></p>
      <h3>Genres</h3>
      <% @genres.each do |genre| %>
        <input id="<%= genre.name %>" type="checkbox" name="genres[]" value="<%= genre.id %>" <%='checked' if @song.genres.include?(genre)%>>
      <% end %>

      <input type="submit" name="" value="Save">
    </form>

  </body>
</html>
